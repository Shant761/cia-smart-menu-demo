rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Phase 1: the browser does not access Firestore directly.
    // CIA Cloud Functions use the Admin SDK and are not affected by these rules.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
