# Nutrition behavior

The smart menu stores and displays calories only (kcal).

USDA matching stores `nutrition.per100g.calories`, and product calculation stores `nutrition.calories` plus `nutrition.per100g.calories` and serving grams. Protein, fat, and carbohydrates are not calculated or stored by the nutrition pipeline.
